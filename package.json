{
  "name": "sample_api",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});\" && parcel build --public-url ./ src/index.html && npx cpx src/science.json dist/ && npx cpx src/test.json dist/",
    "start": "parcel serve src/index.html",
    "deploy": "npm run build && node -e \"const fs=require('fs');fs.rmSync('docs',{recursive:true,force:true});fs.cpSync('dist','docs',{recursive:true});\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "2.12.0"
  },
  "dependencies": {
    "cpx": "1.5.0"
  }
}
