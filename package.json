{
  "name": "sample_api",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel  build --public-url https://johnpaul-bodino.github.io/nyt-apsdev src/index.html && npx cpx src/science.json dist/ && npx cpx src/test.json dist/",
    "start": "parcel serve src/index.html"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "parcel": "2.12.0"
  },
  "dependencies": {
    "cpx": "1.5.0"
  }
}
